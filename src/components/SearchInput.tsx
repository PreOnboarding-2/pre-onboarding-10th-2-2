import React from "react";

const SearchInput = () => {
  return (
    <section>
      <input placeholder="검색어를 입력해주세요." />
      <button>검색</button>
    </section>
  );
};

export default SearchInput;
